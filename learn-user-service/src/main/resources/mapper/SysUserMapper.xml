<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zichen.mapper.SysUserMapper">

    <resultMap id="SysUserMap" type="com.zichen.pojo.SysUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="nickname"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByUsername" resultMap="SysUserMap">
        SELECT * FROM sys_user WHERE username = #{username} LIMIT 1
    </select>

    <select id="selectById" resultMap="SysUserMap">
        SELECT * FROM sys_user WHERE id = #{id} LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user(username, password_hash, nickname, status)
        VALUES(#{username}, #{passwordHash}, #{nickname}, #{status})
    </insert>

</mapper>
